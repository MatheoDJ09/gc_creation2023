{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
    {{ form_row(form.isActive)}}
    <div class="d-felx flex-wrap">
        <div class="col-12 col-md-6 px-3"> 
        {{ form_label(form.title)}}
        {{ form_widget(form.title)}}
        {{ form_help(form.title)}}
        </div>
        <div class="col-12 col-md-6 px-3"> 
        <div class="">{{ form_row(form.text)}}</div>
    </div>
</div>
    {% include 'admin_common/_required-fields.html.twig' %}
    <button class="btn btn-success lg-3 mt-3">{{ button_label|default('Sauvegarde') }}</button>
{{ form_end(form) }}